create table users(
id int primary key not null identity,
fname varchar(100),
lname varchar(100),
address varchar(100),
city varchar(100),
state varchar(100),
zip varchar(100)
)

select * from users


insert into users(fname, lname, address, city, state, zip) 
values('Suraj','Vishwakarma','Thane East','Thane', 'MH','400603')